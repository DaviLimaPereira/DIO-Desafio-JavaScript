function exclusivo(array) {
    console.log('Array antigo: ' + array);
    const mySet = new Set(array);

    mySet.
    return [...mySet];
}

const meuArray = [30, 30, 40, 5, 223, 2049, 3034, 5];

console.log('Novo Array: ' + exclusivo(meuArray));